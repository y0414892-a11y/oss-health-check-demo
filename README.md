# oss-health-check-demo

Demo repository for testing OSS Health Check.

## Installation

No installation is required for this demo repository.

## Usage

This repository uses GitHub Actions to run OSS Health Check on every push to
the `main` branch.

## Example

When the workflow runs, OSS Health Check scans this repository and reports
whether common open-source project files are present.
